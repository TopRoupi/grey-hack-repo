<div class="my-2 px-2 <%= @link ? "w-1/3" : "w-full" %> overflow-hidden">
<!-- pure go horse i will do this properly later :v  -->
  <a
    href="<%= @link ? category_path(@category) : root_path %>"
    class="bg-beaver-800 hover:bg-beaver-700 py-4 text-white text-beaver-300  rounded-lg w-full flex flex-col justify-center md:flex-row items-center"
    >
    <%= octicon @category.icon, width: 55, class: "fill-current m-auto md:m-0 md:mr-6" %>
    <div class="my-auto text-center text-2xl text-beaver-400 font-semibold">
      <%= @category.name %>
    </div>
  </a>
</div>
