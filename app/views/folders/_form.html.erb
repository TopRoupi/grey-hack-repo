<turbo-frame id="<%= dom_id folder %>">
  <% if defined? message %>
    <%= render Layout::Alert.new(message) %>
  <% end %>
  <%= form_for(folder, builder: DaisyFormBuilder) do |f| %>
    <%= f.group :name do %>
      <%= f.text_field :name %>
    <% end %>

    <%= f.submit %>
  <% end %>
</turbo-frame>
